import { AmendedMessagePipe } from './amended-message.pipe';

describe('AmendedMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new AmendedMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
